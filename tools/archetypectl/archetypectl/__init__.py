"""archetypectl -- platform tooling for the archetype model.

Only the ``enrich`` subcommand exists so far (roadmap phase 2c.2). ``resolve``
and ``version`` are referenced by the documents and land later; the package is
laid out so they are new modules, not a rewrite.
"""

__version__ = "0.1.0"
